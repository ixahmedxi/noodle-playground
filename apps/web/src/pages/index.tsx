import { Typography } from '@noodle/ui';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <main>
      <Typography variant="h1">Home page</Typography>
    </main>
  );
};

export default Home;
